use project::qrtext::{QRDetails, QrText};
use std::fs::read_to_string;
use std::path::Path;
use tempfile::TempDir; // Adjust this path based on your actual module structure

#[cfg(test)]
mod tests {}
